# Frontend environment variables (Vercel / local)
# Set in Vercel project settings before building.

# Public backend API URL (no trailing slash). Required for production builds.
NEXT_PUBLIC_API_URL=http://localhost:4000
